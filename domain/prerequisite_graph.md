# Prerequisite Graph — Event Fixture

```text
Binary Tree Inorder Traversal
├── tree structure basics
├── traversal ordering
└── recursion
    └── function-call / stack reasoning
```

The agent may hypothesize `recursion` from current evidence plus learner history, but must test it rather than assume it.
